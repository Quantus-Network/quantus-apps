import 'package:quantus_cold_wallet/generated/version.g.dart';

const updateAppHint = 'Maybe you need to update your cold wallet app.';
const currentAppVersion = 'Current app version $appVersion build $appBuildNumber';
