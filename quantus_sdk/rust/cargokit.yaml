# cargokit.yaml
cargo:
  release:
    toolchain: nightly
    extra_args:
      - -Zbuild-std=std,panic_abort
      - -Zbuild-std-features=panic_immediate_abort
  debug:
    toolchain: nightly
    extra_args:
      - -Zbuild-std=std,panic_abort
      - -Zbuild-std-features=panic_immediate_abort
